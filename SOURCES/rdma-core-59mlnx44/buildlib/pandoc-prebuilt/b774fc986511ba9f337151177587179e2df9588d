.\" Automatically generated by Pandoc 1.16.0.2
.\"
.TH "IBV_QUERY_GID_TABLE" "3" "2020\-04\-24" "libibverbs" "Libibverbs Programmer\[aq]s Manual"
.hy
.SH NAME
.PP
ibv_query_gid_table \- query an InfiniBand device\[aq]s GID table
.SH SYNOPSIS
.IP
.nf
\f[C]
#include\ <infiniband/verbs.h>

ssize_t\ ibv_query_gid_table(struct\ ibv_context\ *context,
\ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ struct\ ibv_gid_entry\ *entries,
\ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ size_t\ max_entries,
\ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ uint32_t\ flags);
\f[]
.fi
.SH DESCRIPTION
.PP
\f[B]ibv_query_gid_table()\f[] returns the valid GID table entries of
the RDMA device context \f[I]context\f[] at the pointer
\f[I]entries\f[].
.PP
A caller must allocate \f[I]entries\f[] array for the GID table entries
it desires to query.
This API returns only valid GID table entries.
.PP
A caller must pass non zero number of entries at \f[I]max_entries\f[]
that corresponds to the size of \f[I]entries\f[] array.
.PP
\f[I]entries\f[] array must be allocated such that it can contain all
the valid GID table entries of the device.
If there are more valid GID entries than the provided value of
\f[I]max_entries\f[] and \f[I]entries\f[] array, the call will fail.
For example, if an RDMA device \f[I]context\f[] has a total of 10 valid
GID entries, \f[I]entries\f[] should be allocated for at least 10
entries, and \f[I]max_entries\f[] should be set appropriately.
.SH ARGUMENTS
.TP
.B \f[I]context\f[]
The context of the device to query.
.RS
.RE
.TP
.B \f[I]entries\f[]
Array of ibv_gid_entry structs where the GID entries are returned.
Please see \f[B]ibv_query_gid_ex\f[](3) man page for
\f[I]ibv_gid_entry\f[].
.RS
.RE
.TP
.B \f[I]max_entries\f[]
Maximum number of entries that can be returned.
.RS
.RE
.TP
.B \f[I]flags\f[]
Extra fields to query post \f[I]entries\->ndev_ifindex\f[], for now must
be 0.
.RS
.RE
.SH RETURN VALUE
.PP
\f[B]ibv_query_gid_table()\f[] returns the number of entries that were
read on success or negative errno value on error.
Number of entries returned is <= max_entries.
.SH SEE ALSO
.PP
\f[B]ibv_open_device\f[](3), \f[B]ibv_query_device\f[](3),
\f[B]ibv_query_port\f[](3), \f[B]ibv_query_gid_ex\f[](3)
.SH AUTHOR
.PP
Parav Pandit <parav@nvidia.com>
